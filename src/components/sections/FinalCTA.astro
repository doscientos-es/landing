---
import { copy } from "~/shared/lib/copy";
import AvailableTag from "../ui/AvailableTag.astro";
import CtaButton from "../ui/CtaButton.astro";
import ClientsMarqueeRail from "../ui/clients/ClientsMarqueeRail.astro";

const { finalCta } = copy;
---

<section id="final-cta" class="px-6">
  <div
    class="max-w-3xl w-full mx-auto text-center flex flex-col justify-center items-center gap-10"
    data-stagger="100"
  >
    <h2
      class="text-2xl lg:text-4xl font-bold text-balance tracking-tight text-foreground max-w-sm md:max-w-xl"
      data-reveal="scale"
      data-speed="normal"
      data-easing="bounce"
    >
      {finalCta.question}
    </h2>

    <!-- Description -->
    <p
      class="text-muted-foreground max-w-sm text-pretty"
      data-reveal="bottom"
      data-speed="normal"
      data-easing="soft"
      data-distance="small"
    >
      {finalCta.description}
    </p>

    <!-- Marquee respeta el max-w-3xl del padre -->
    <div
      class="w-full"
      data-reveal="scale"
      data-speed="fast"
      data-easing="smooth"
    >
      <ClientsMarqueeRail />
    </div>

    <div data-reveal="scale" data-speed="fast" data-easing="elastic">
      <CtaButton label={finalCta.cta} />
    </div>

    <div data-reveal="bottom" data-speed="normal" data-easing="soft">
      <AvailableTag />
    </div>
  </div>
</section>
